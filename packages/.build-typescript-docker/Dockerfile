# TypeScript Development Dockerfile for Bolt
# Base image: Node.js 22 Alpine (lightweight)
FROM node:22-alpine

# Install git (required for some npm packages)
RUN apk add --no-cache git

# Set working directory
WORKDIR /project

# Default command: shell for interactive use
CMD ["/bin/sh"]
